package view;

/**
 * Choices Enumeration.
 */
public enum Choices {
  Exit, addBoat, addMember, removeBoat, removeMember, showMemberBoat, showAllMember, showMemberInfo, changeMemberInfo,
  ChangeBoatInfo
}
